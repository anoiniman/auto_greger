local module = {}
local MSBuilder, Goal, Requirement = require("reasoning.MetaScript")

local item = nil
local filter = nil
local num = 0

-- initial log constraint
item = "log"
filter = "loose"
num = 32

MSBuilder.

module[1] = MSBuilder.build()

return module
